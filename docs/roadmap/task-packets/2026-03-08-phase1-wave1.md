# Phase 1 Wave 1 Task Packets (2026-03-08)

Use these packets as copy/paste-ready dispatch inputs for implementer and spec-review subagents.

## W1-01: Read vs Write Primitive Taxonomy

- Lane: `Safety Boundary`
- Worktree: `.codex/worktrees/phase1-safety-boundary`
- Priority: `P0`
- Goal: Define explicit browser primitive classes (`read`, `write`) and ensure all existing primitives are classified.

Acceptance criteria:
- Primitive catalog contains class per primitive.
- `write` class is explicitly documented as restricted for MCP v1 exposure.
- Unit tests verify classification is complete and no primitive is unclassified.

Required verification:
- `npm test`

Dependencies:
- None.

## W1-02: Enforce Read-Only MCP v1 Surface

- Lane: `Safety Boundary`
- Worktree: `.codex/worktrees/phase1-safety-boundary`
- Priority: `P0`
- Goal: Enforce hard guardrails so MCP v1 exposes only read primitives.

Acceptance criteria:
- Tool registration path rejects write primitive exposure in v1.
- Contributor-facing docs state boundary as intentional policy.
- Tests fail if a write primitive is added to MCP v1 surface.

Required verification:
- `npm test`

Dependencies:
- `W1-01` [hard].

## W1-03: Source Shape Contract Schema

- Lane: `Source Contracts`
- Worktree: `.codex/worktrees/phase1-source-contracts`
- Priority: `P0`
- Goal: Add definitive schema for source search-parameter expectations and extraction shape (`required` + `optional` metadata).

Acceptance criteria:
- Schema includes required/optional metadata fields and allowed value semantics.
- Search parameter expectations are represented explicitly.
- Invalid contracts fail validation with actionable diagnostics.

Required verification:
- `npm test`

Dependencies:
- None.

## W1-04: Contract Loader + Drift Diagnostics

- Lane: `Source Contracts`
- Worktree: `.codex/worktrees/phase1-source-contracts`
- Priority: `P0`
- Goal: Load contracts in pipeline and emit diagnostics when payloads drift from expected shape.

Acceptance criteria:
- Contract loader active in relevant ingest path.
- Drift diagnostics persisted with field-level mismatch context.
- Tests cover healthy payload, missing required field, and stale/noisy payload behavior.

Required verification:
- `npm test`

Dependencies:
- `W1-03` [hard].

## W1-05: Dashboard Formatter Diagnostics Visibility

- Lane: `Criteria Fidelity`
- Worktree: `.codex/worktrees/phase1-criteria-fidelity`
- Priority: `P1`
- Goal: Complete dashboard surfacing for persisted formatter diagnostics.

Acceptance criteria:
- Diagnostics appear in dashboard for supported run contexts.
- Empty-state behavior is clear when no diagnostics exist.
- Tests cover CLI persistence + dashboard rendering path.

Required verification:
- `npm test`

Dependencies:
- None.

## W1-06: Multi-Keyword Criteria End-to-End

- Lane: `Criteria Fidelity`
- Worktree: `.codex/worktrees/phase1-criteria-fidelity`
- Priority: `P1`
- Goal: Finish multi-keyword support through search criteria to source URL construction.

Acceptance criteria:
- Comma-separated keyword input is normalized consistently.
- URL builders receive normalized keyword sets.
- Behavior documented for AND/OR follow-up extensions.

Required verification:
- `npm test`

Dependencies:
- Search controls cleanup [soft].

## W1-07: Canonical Event Schema + PostHog Mapping

- Lane: `Operations Metrics`
- Worktree: `.codex/worktrees/phase1-operations-metrics`
- Priority: `P1`
- Goal: Define and wire canonical event schema across `terminal`, `dashboard`, `codex`, `claude`.
- Setup note: PostHog SDK/instrumentation already exists; focus this task on schema consistency, channel coverage, and reporting contracts.

Acceptance criteria:
- Event schema includes channel tags and identity mode.
- Mapping to PostHog events is documented and implemented in metering path.
- Tests validate schema serialization and key metric counters.

Required verification:
- `npm test`

Dependencies:
- None.

## W1-08: Status-Aware Retention Policy Wiring

- Lane: `Operations Metrics`
- Worktree: `.codex/worktrees/phase1-operations-metrics`
- Priority: `P1`
- Goal: Implement policy defaults for automatic deletion by status while preserving `applied`.

Acceptance criteria:
- Default retention: `new=30`, `viewed=45`, `skip_for_now=21`, `rejected=14`, `applied=never`.
- Policy can be inspected/overridden by user settings path.
- Cleanup audit includes deleted counts and protected item behavior.

Required verification:
- `npm test`

Dependencies:
- `W1-07` [soft].
