# 2026-03-30

## Source Table Semantics

- Implemented v2 source-run semantics for the Sources table.
- Added persisted per-run fields in `source_run_deltas`:
  - `raw_found_count`
  - `hard_filtered_count`
  - `duplicate_collapsed_count`
  - `imported_kept_count`
- Switched CLI and review-server sync paths to compute user-facing source metrics from the shared evaluation and identity funnel instead of legacy pre-import cleanup counts.
- Updated source-row aggregation so public `Found`, `Filtered`, `Dupes`, and `Imported` use v2 metrics only.
- Preserved unknowns for legacy-only sources instead of coercing misleading zeros.

## YC Identity

- Fixed `YC Jobs` to use job-level review targets and identity:
  - `https://www.workatastartup.com/jobs/<job-id>`
- Stopped treating YC company pages as canonical job identity, which had been inflating duplicate counts.

## Verification

- Passed:
  - `node --test test/run-deltas.test.js test/normalize-dedupe.test.js test/yc-jobs.test.js test/yc-capture.test.js test/review-searches-react-logic.test.js`
  - `node --test test/review-source-data-quality.test.js test/dashboard-api-contract.test.js`
  - `node -c src/cli.js`
  - `node -c src/review/server.js`
  - `node -c src/jobs/repository.js`
  - `node -c src/jobs/normalize.js`
  - `node -c src/sources/yc-jobs.js`
  - `node -c src/review/web/src/features/searches/logic.js`
  - `npm run dashboard:web:build`

## Remaining

- Fixed the second-order semantic bug where same-source reruns were being counted as `Dupes`, which collapsed `Imported` to zero for rerun-heavy sources.
- Added `semantics_version` to `source_run_deltas` and gated public source-row aggregation to `semantics_version = 2` so old broken metric rows no longer pollute the Sources table.
- Folded the controller branch into `/Users/admin/job-finder` `qa/current`, reran `node src/cli.js sync`, restarted QA, and verified live source rows at `http://127.0.0.1:4311` now come only from the corrected metric version:
  - `LinkedIn|18|0|2|16`
  - `Built In|0|0|0|0`
  - `Indeed|4|0|0|4`
  - `ZipRecruiter|13|0|0|13`
  - `YC Jobs|30|0|0|30`
  - `Levels.fyi|26|0|0|26`
- Remaining product issue is no longer the semantic mixing bug; the next discussion should be whether the latest per-source runs themselves are too broad, not whether the table is summing broken rows.

## Live QA Path

- Root-caused the latest mixed batch: LinkedIn captured live but was silently quarantined, while Indeed and Zip were cache-served. That made the run look coherent while hiding the actual current source behavior.
- Added a QA-only override in `src/sources/qa-mode.js`.
- `scripts/review-qa.sh` now exports `JOB_FINDER_SOURCE_QA_MODE=1`.
- In stakeholder QA mode:
  - browser sources force live capture with `refreshProfile: probe`
  - browser sources bypass cache with `forceRefresh: true`
  - quarantine no longer silently blocks sync
- Fixed the follow-on handoff bug where a fresh browser capture was being relabeled as `cache_fresh` during sync because refresh state was recomputed after capture. Source rows now preserve `servedFrom: live` and `statusReason: fetched_during_sync` for the current batch when the captured timestamp matches the latest successful attempt.
- Added focused regression coverage:
  - `test/source-qa-mode.test.js`
  - `test/review-run-all-resilience.test.js`
  - `test/dashboard-refresh-status.test.js`
- Verification passed:
  - `node --test test/review-run-all-resilience.test.js test/source-qa-mode.test.js test/dashboard-refresh-status.test.js test/capture-validation.test.js`
  - `node -c src/review/server.js`
  - `node -c src/cli.js`
  - `node -c src/sources/qa-mode.js`
  - `npm run dashboard:web:build`
