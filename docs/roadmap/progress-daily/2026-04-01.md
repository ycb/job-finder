# 2026-04-01

## LinkedIn extraction refactor

- Wrote and started executing the dedicated LinkedIn extraction refactor ExecPlan:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/docs/plans/2026-04-01-linkedin-extraction-refactor-execplan.md`
- Reworked LinkedIn capture in the controller branch to be summary-card-first:
  - browser script now emits row snapshots instead of assuming every visible DOM seed is a usable job
  - Node-side finalization now skips placeholder rows, rejects mismatched detail ids, and records capture diagnostics
  - LinkedIn capture persistence no longer depends on generic full-detail enrichment for MVP import
- Added regression coverage for:
  - hydrated row detection
  - placeholder skipping
  - detail-id mismatch rejection
  - capture diagnostics payload shape

## Verification

- Passed:
  - `node --test test/linkedin-chrome-extraction.test.js test/linkedin-capture-pagination.test.js`
  - `node --test test/linkedin-chrome-extraction.test.js test/linkedin-capture-pagination.test.js test/review-source-data-quality.test.js test/dashboard-refresh-status.test.js`
  - `node -c src/browser-bridge/providers/chrome-applescript.js`
  - `node -c src/sources/linkedin-saved-search.js`
  - `npm run dashboard:web:build`

## Remaining gap

- Live verification is not fully closed yet in the controller worktree.
- A QA-bridge run captured `24` LinkedIn jobs, which is materially better than the recent broken `5-11` range, but that run was serviced by the existing bridge under `/Users/admin/job-finder`.
- Direct controller-side AppleScript verification still needs a same-checkout bridge or a QA fold-in before final acceptance can be claimed.

## LinkedIn structured-payload POC

- Added a narrow POC proving the saved LinkedIn sample page exposes a hidden structured-data surface we can parse without relying on hydrated left-rail DOM:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/src/sources/linkedin-structured-payload.js`
  - `/Users/admin/.codex/worktrees/51f6/job-finder/scripts/linkedin-structured-payload-poc.js`
  - `/Users/admin/.codex/worktrees/51f6/job-finder/test/linkedin-structured-payload.test.js`
- Verified the sample extracts the selected job with stable MVP fields:
  - `externalId`
  - canonical `/jobs/view/<id>/` URL
  - title
  - company
  - location
  - posted recency
  - workplace type
- This proves a structured extraction surface exists on the sample page, but the sample does not yet prove that the full search result set is embedded in the same way.
- LinkedIn structured-payload live POC succeeded against the first search page. Hidden `voyagerJobsDashJobCards` data yielded `7` real jobs with stable ids/titles/companies, proving the approach is feasible but only for an initial slice rather than the full visible first page.
- Follow-up scroll experiment on the same LinkedIn first page showed the hidden structured payload stayed at `7` before and after scroll. The main-document candidates exposed no usable scroll capacity, so lazy loading is not currently materializing into the structured payload surface we are reading.

## LinkedIn live diagnostic POC

- Added a dedicated live diagnostic POC and helper layer:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/src/sources/linkedin-diagnostic.js`
  - `/Users/admin/.codex/worktrees/51f6/job-finder/scripts/linkedin-live-diagnostic-poc.js`
  - `/Users/admin/.codex/worktrees/51f6/job-finder/test/linkedin-diagnostic.test.js`
- Verified the pure helper and job-id extraction logic:
  - `node --test test/linkedin-diagnostic.test.js test/linkedin-structured-payload.test.js`
  - `node -c src/sources/linkedin-diagnostic.js`
  - `node -c scripts/linkedin-live-diagnostic-poc.js`
- Ran the live canonical search diagnostic and proved the richer LinkedIn first-page result set is present in the runtime resource layer rather than in `outerHTML`:
  - `rowIdCount = 0`
  - `structuredCount = 7`
  - `resourceJobCount = 25`
  - `activationAttemptCount = 0`
- The most important resource evidence came from live URLs like:
  - `voyagerJobsDashJobCards ... count=25 ... start=0`
  - `voyagerJobsDashJobCards ... count=25 ... start=25`
  - `prefetchJobPostingCardUrns:List(...)`
- Conclusion: the current LinkedIn under-harvest is a surface-selection bug. The page lifecycle knows about far more jobs than the current document/payload parser sees, so the next refactor should target runtime resource/state data rather than more visible-card DOM tuning.

## LinkedIn resource-layer POC

- Added a second POC to fetch and parse the live `voyagerJobsDashJobCards` response body from the page context:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/scripts/linkedin-resource-layer-poc.js`
- Extended the structured parser so it can read both:
  - hidden BPR payloads
  - normalized `metadata / elements / paging` API response bodies
- Added parser coverage for normalized LinkedIn recipe objects:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/test/linkedin-structured-payload.test.js`
- Live result on the canonical search:
  - first raw resource fetch returned `403`
  - adding the page's CSRF token plus `x-restli-protocol-version: 2.0.0` unlocked the response
  - final fetch returned `200`
  - parsed first-page result set: `25` jobs
- This is the key proof:
  - hidden structured payloads only yielded `7`
  - resource-layer `job-cards` response yielded `25`
  - the data is regular and complete enough for MVP import from the runtime resource layer
- Practical implication:
  - the next LinkedIn refactor should replace visible-card-first extraction with resource-layer-first extraction
  - detail pane and visible DOM become secondary enrichment/verification surfaces rather than the primary data source
- Verified live LinkedIn QA capture via resource-layer extraction reached `82 / 82` jobs. The remaining LinkedIn issue is not extraction but source-table semantics: `buildSourceRunSemanticMetrics()` was still counting generic reject-bucket rows as `Filtered`, contrary to the approved product contract.
