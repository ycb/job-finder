- Added the shared direct HTTP source registration checklist so future source builds can start from a reusable contract instead of re-deriving registration expectations.
- Extended the source library and schema/cache-policy surfaces for the approved MVP launch-build sources:
  - `YC Jobs`
  - `Levels.fyi`
- Added registration-focused tests for both launch-build sources plus source-contract acceptance coverage for the new types.
- Added the controller rule that registration-lane work must leave behind a lightweight checklist covering source-library entry, schema acceptance, cache TTL, and reporting expectations.
