- Added the shared direct HTTP source registration checklist so future source builds can start from a reusable contract instead of re-deriving registration expectations.
- Extended the source library and schema/cache-policy surfaces for the approved MVP launch-build sources:
  - `YC Jobs`
  - `Levels.fyi`
- Added registration-focused tests for both launch-build sources plus source-contract acceptance coverage for the new types.
- Added the controller rule that registration-lane work must leave behind a lightweight checklist covering source-library entry, schema acceptance, cache TTL, and reporting expectations.
- Corrected the controller workflow after a missed publish gate: the controller branch now treats `push -> QA instructions -> roadmap/reporting update` as a single required cycle instead of stopping at local integration.
- Added a repo-local sprint controller skill to codify parallel lane oversight, rolling integration, push gates, QA handoff expectations, and roadmap/reporting duties.
- Pruned the local source library/example config to the approved six-source MVP slate and updated onboarding map-mode coverage accordingly.
- Corrected post-integration source regressions for the new MVP sources: `YC Jobs` now reuses the shared auth-required modal flow, and new direct HTTP sources no longer introduce a bespoke `live source` user-facing status label.
- Completed product-integration parity for the two new MVP sources at the shared-contract layer:
  - `YC Jobs` and `Levels.fyi` now register as first-class source kinds in search/status logic instead of falling through as `unknown`
  - dashboard source rows now carry internal-only `noveltyDiagnostics` for source evaluation while keeping public UI unchanged
- Added reusable source-type artifacts for future source work:
  - [YC Jobs notes](/Users/admin/.codex/worktrees/51f6/job-finder/docs/roadmap/yc-jobs-http-direct-notes.md)
  - [Levels.fyi notes](/Users/admin/.codex/worktrees/51f6/job-finder/docs/roadmap/levelsfyi-http-direct-notes.md)
- Locked internal novelty accounting to the approved default baseline:
  - `LinkedIn + Indeed`
