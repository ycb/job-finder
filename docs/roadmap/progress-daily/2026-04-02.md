# 2026-04-02

## Review language

- Updated user-facing score-bucket language so low-score rows are no longer framed as true rejects:
  - `high_signal` -> `Best match`
  - `medium_signal` / `review_later` -> `Possible match`
  - `low_signal` / `reject` -> `Low signal`
- Manual job status `Rejected` remains unchanged.

## Verification

- Passed:
  - `node --test test/review-jobs-react-ui-model.test.js test/review-jobs-react-logic.test.js`
  - `npm run dashboard:web:build`

## Source parity re-check

- Re-validated the current ZipRecruiter path directly against live QA:
  - builder output for current QA criteria: `https://www.ziprecruiter.com/jobs-search?search=Product+manager+ai&location=San+Francisco%2C+CA&page=1`
  - fresh live capture via `node src/cli.js capture-source-live zip-ai-pm --force-refresh` returned `60` jobs
  - top captured roles are plausible and aligned with the manual search intent (`Principal Product Manager, GPUs and AI Accelerators`, `Product Manager - Conversational AI`, `Senior Product Manager, AI`, etc.)
- Re-validated the current Indeed path directly against live QA:
  - builder output for current QA criteria: `https://www.indeed.com/jobs?q=Product+manager+ai&l=San+Francisco%2C+CA&radius=25&salaryType=%24200%2C000&fromage=3`
  - fresh live capture via `node src/cli.js capture-source-live indeed-ai-pm --force-refresh` returned `19` jobs
  - captured rows were plausible Bay Area roles and did not include the old `/career/` salary-page pollution

## Current read

- LinkedIn extraction is materially restored (`82 / 82`) and no longer the primary blocker.
- ZipRecruiter is no longer behaving like the broken `0-4` result regression that kicked off the recent source-recovery work.
- Indeed is no longer catastrophically broken, but its query construction is still compressed enough that manual/native parity should be treated as unfinished rather than signed off.
- The next highest-value work is now the source-map/manual-parity closeout across the full MVP slate, plus remaining run-honesty/reporting cleanup.

## MVP parity matrix

- Completed a fresh full QA run with `node src/cli.js run --force-refresh` and recorded a source-by-source pass/fail matrix in:
  - `/Users/admin/.codex/worktrees/51f6/job-finder/docs/analysis/2026-04-02-mvp-source-parity-matrix.md`
- Current read from that matrix:
  - `LinkedIn`: pass on extraction, partial on query-contract polish
  - `Built In`: partial, acceptable only if unsupported `minSalary` is explicitly accepted
  - `Indeed`: partial, plausible but still compressed
  - `ZipRecruiter`: partial, live again but still not full query-contract parity
  - `Levels.fyi`: fail on query/narrowing quality
  - `YC Jobs`: fail on query-contract support
- That shifts the next implementation priority away from emergency LinkedIn/Zip rescue and toward:
  1. `YC Jobs` criteria mapping
  2. `Levels.fyi` narrowing/query quality
  3. `ZipRecruiter` query-parity cleanup
  4. `Indeed` manual/native parity signoff
# 2026-04-02

- Revised `YC Jobs` recovery from direct-fetch builder patching to browser/app-state capture after proving the logged-in page exposes the authoritative jobs array in `data-page.props.jobs`.
- Implemented controller-side `yc_jobs` browser-capture support:
  - added YC to browser-capture dispatch in CLI/server
  - added Chrome AppleScript capture path that reads `data-page`
  - updated `yc_jobs` parsing to honor explicit search state instead of route-only inference
  - updated YC source contract from mostly `unsupported` to honest `url` + `post_capture` semantics
- Verification:
  - `node --test test/yc-jobs.test.js test/source-criteria-accountability.test.js test/search-url-builder.test.js test/source-contracts.test.js test/yc-capture.test.js`
  - `node --test test/review-run-all-resilience.test.js test/yc-source-registration.test.js test/source-auth-probe.test.js`
  - syntax checks on `src/sources/yc-jobs.js`, `src/sources/search-url-builder.js`, `src/browser-bridge/providers/chrome-applescript.js`, `src/cli.js`, `src/review/server.js`
- Live controller-side evidence:
  - generic logged-in YC browser capture returned `22` jobs from app state
  - synthetic canonical PM + AI + SF + 3d + $200k browser capture returned `3` jobs:
    - `Staff Product Manager, Growth`
    - `Staff Product Manager, Identity Risk Solutions`
    - `Product Lead`
