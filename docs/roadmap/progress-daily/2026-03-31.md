# 2026-03-31

- Created [2026-03-31-source-regression-root-cause-execplan.md](/Users/admin/.codex/worktrees/51f6/job-finder/docs/plans/2026-03-31-source-regression-root-cause-execplan.md) to trace the current source regressions against the last known-good baseline instead of continuing generic tuning.
- Wrote [2026-03-31-mvp-source-regression-baseline.md](/Users/admin/.codex/worktrees/51f6/job-finder/docs/analysis/2026-03-31-mvp-source-regression-baseline.md) with per-source generated URLs, live semantic rows, and raw capture evidence.
- Confirmed the LinkedIn row collapse was caused by the sync path counting from `collectJobsFromSource()` after `applySourceHardFilters()`. Added `collectRawJobsFromSource()` and switched sync/CLI accounting to use the raw capture rows.
- Restored richer canonical base URLs for `LinkedIn`, `Indeed`, and `ZipRecruiter` in the source library and updated the builder to preserve richer existing location state when criteria only specifies the city name.
- Added regression coverage for:
  - raw-vs-filtered LinkedIn source collection
  - Zip query construction preserving richer native search state
- Fixed a dashboard regression where source rows mixed `source_run_deltas` counts with stale `refresh-state` status/servedFrom fields. Source rows now prefer the latest run row for status unless a later failed attempt supersedes it.
- Fixed the remaining source-table semantics bug where `Filtered` only counted the scorer's narrow `hardFiltered` flag. Source rows now treat all criteria-based `bucket='reject'` evaluations as filtered, which makes LinkedIn/Indeed/Zip source rows reflect actual search rejection instead of showing misleading zeros.
- Confirmed that `node src/cli.js sync` is not a live recapture path for browser sources, so live QA validation must go through the dashboard `run-all` flow. A fresh `run-all` batch (`cab1d878-7b2f-47a2-825e-556d0997af6f`) showed: LinkedIn capture failed with a Chrome AppleScript timeout and fell back to stale capture metadata; Indeed captured `14 -> 12 filtered -> 2 imported`; Zip captured a live page advertising `27` results but only extracted `4` rows, which points to an extraction shortfall after the query lands on the source page.
- Fixed `src/review/server.js runSyncAndScore()` so browser-source sync now rereads the refreshed capture summary after collection instead of evaluating fresh raw jobs against stale `capturedAt`, `expectedCount`, and `captureFunnel` metadata. Added `test/review-sync-capture-summary.test.js` to lock the regression down.
- Confirmed with live Zip probes that the page-2 `/jobs-search/2` URL is not the primary root cause: Zip canonicalizes `?page=2` to that path itself. The stronger evidence now is query-state overconstraint: the generated query produces `4` results live, while removing `days=3` jumps to `258` and removing both `days=3` and salary jumps to `338`.
- Fixed a second LinkedIn regression in the browser extractor: stale open detail panes were being attached to unrelated cards. LinkedIn detail descriptions are now trusted only when the detail pane job id matches the card job id, and a regression test was added to prevent cross-card detail contamination.
- Proved the current Zip regression is query-state overconstraint, not a dead extractor. A live direct capture against the simplified manual-equivalent Zip URL (`search + location + page=1`) captured `57` jobs, while the current configured URL with date/salary/radius/employment constraints had been yielding `4`.
- Simplified the Zip source contract and builder to match that evidence: Zip now keeps only title/keywords/location in the URL and applies salary/date/distance/experience via post-capture evaluation instead of overconstraining the native search state.
- Identified the current LinkedIn navigation regression: the browser extractor was clicking the job anchor to read detail hints, which can navigate out of `/jobs/search` into `/jobs/collections/similar-jobs`. Switched the detail-read interaction to stay on the card/search-results context and added a regression guard that aborts/recoveries if the page leaves the search-results URL family.
