- Implemented persisted queue semantics so the Jobs workspace can separate:
  - `New` = jobs imported in the latest completed import batch
  - `Unread` = jobs never opened
- Added persistence and backfill for:
  - `jobs.last_import_batch_id`
  - `applications.first_viewed_at`
- Updated both React and legacy jobs UI flows so `Unread` is a first-class queue filter distinct from `New`.
- Reused `source_run_deltas.run_id` as the durable import-batch marker instead of creating a second queue-batch table.
- Closed the shared builder/runtime gap for the new MVP sources:
  - `levelsfyi_search` now has an explicit shared builder branch and truthful criteria-accountability output
  - `yc_jobs` now has an explicit shared builder branch for the fixed product-manager route
  - generic source collection dispatch now includes both `Levels.fyi` and `YC Jobs`
- Expanded the shared source-contract model to cover the full current product criteria surface:
  - `keywordMode`
  - `hardIncludeTerms`
  - `includeTerms`
  - `excludeTerms`
- Fixed active MVP accountability drift:
  - `Indeed` and `ZipRecruiter` now report `hardIncludeTerms` truthfully when those terms are folded into the runtime query text
- Added internal audit artifacts:
  - [MVP source-map audit](/Users/admin/.codex/worktrees/51f6/job-finder/docs/analysis/2026-03-27-mvp-source-map-audit.md)
  - [Source-map acceptance checklist](/Users/admin/.codex/worktrees/51f6/job-finder/docs/backlog-specs/p1-source-map-acceptance-checklist.md)
- Verification completed:
  - `node --test test/review-jobs-react-logic.test.js test/review-jobs-api.test.js test/review-jobs-react-ui-model.test.js test/run-deltas.test.js test/source-criteria-accountability.test.js test/source-contracts.test.js test/levelsfyi-jobs.test.js`
  - `npm run dashboard:web:build`
- Follow-up controller review found and fixed three integration gaps before QA:
  - `New` now clears after a zero-import completed run instead of staying pinned to the last importing run
  - the legacy dashboard optimistic viewed path now updates `isUnread`/`firstViewedAt`, not only `status`
  - shared React jobs logic now recognizes `Levels.fyi` and `YC Jobs` as first-class source kinds
- Additional verification completed:
  - `node --test test/dashboard-api-contract.test.js test/review-source-data-quality.test.js`
