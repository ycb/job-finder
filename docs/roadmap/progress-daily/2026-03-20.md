# Daily Progress - 2026-03-20

## Focus
- Re-baseline the source-data-quality pass.
- Restore roadmap-process hygiene after drift toward backlog/ExecPlan-only updates.
- Align MVP source scope to the approved 6-source slate.
- Switch execution from serial source debugging to controller-branch parallel lanes.

## Completed
- Clarified that filtered Google Jobs should be treated as a real source adapter if revisited later, but it is not part of the approved MVP source slate.
- Confirmed P0 source-data-quality plumbing is in good shape in the active branch:
  - per-source `run-all` resilience
  - last-attempt vs last-success reporting fields
  - Cloudflare / `additional verification needed` challenge classification
  - suppression of bogus unknown expected counts
- Updated the source-data-quality ExecPlan to reflect source-type framing:
  - `Built In` as baseline
  - `Google` as non-MVP adapter analysis only
  - `Ashby` novelty spike / likely defer
- Began backlog realignment to the approved MVP source slate:
  - promote `YC Jobs` to `P1`
  - promote `Levels.fyi` to `P1`
  - add ZipRecruiter direct-job URL fix as a scoped blocker
  - keep Wellfound / Greenhouse / Ashby / RemoteOK out of MVP
- Added source-slate artifacts and decision-log coverage so the approved six-source MVP policy is explicit in roadmap docs, not only in backlog/spec drafts.
- Added a new MVP core-functionality backlog item to analyze and explicitly verdict the launch scoring model instead of assuming it is either fine or broken.
- Switched to explicit controller branch:
  - `codex/controller-source-data-quality`
- Launched six concurrent lanes with roadmap evidence:
  - LinkedIn
  - Google Jobs
  - Indeed challenge/reporting
  - Ashby novelty spike
  - Built In baseline rubric
  - shared search-construction audit
- Reviewed and integrated the returned LinkedIn lane (`L1`) into the controller branch:
  - hard include terms now participate in LinkedIn query construction
  - polluted LinkedIn company/description cleanup improvements are present
  - targeted verification passed: `20/20`
- Reviewed and integrated the returned Indeed/reporting lane (`L3`) into the controller branch:
  - challenge classification covers Cloudflare / `additional verification needed`
  - bogus expected totals are suppressed
  - latest-attempt metadata is exposed alongside last-success metadata
  - targeted verification passed: `22/22`
- Received back the following lane outcomes:
  - `Google Jobs`: implementation spike returned, but remains outside MVP
  - `Ashby`: recommend defer from MVP
  - `Built In`: confirmed baseline source
  - shared search-construction audit: confirms upstream source-construction failures are material
- Re-scoped the active controller execution model to the approved six-source MVP slate:
  - `LinkedIn`
  - `Built In SF`
  - `Indeed`
  - `ZipRecruiter`
  - `YC Jobs`
  - `Levels.fyi`
- Updated the dispatch board and parallel handoff packet so the six live lanes now map directly to the six MVP source gates instead of the earlier mixed-source analysis slate.
- Re-checked the Indeed MVP lane against the current controller branch:
  - no additional implementation was needed
  - degraded-but-honest behavior is already present and verified in a fresh targeted suite (`30/30`)
  - remaining risk is limited to future live Cloudflare wording drift
- Integrated the returned ZipRecruiter blocker fix into the controller branch:
  - posting-specific `lk=` / `uuid` deep links now survive normalization and review-target resolution
  - targeted verification covering Zip deep links, normalization, Built In guard behavior, and source reporting passed (`25/25`)
- Integrated the Built In baseline guard lane into the controller branch:
  - added a regression rubric artifact for the direct non-auth baseline
  - the Built In source now has explicit guard coverage and remains the healthy reference source
- Closed the completed controller lanes after integration (`LinkedIn`, `Indeed/reporting`, `ZipRecruiter`, `Built In`) so the active lane inventory reflects reality instead of leaving stale workers marked as open.
- Re-framed the remaining MVP work as two supervised build lanes:
  - `YC Jobs`: adapter/parser/capture work is now present and its targeted adapter tests pass; the remaining work is the shared registration/schema/cache-policy surface
  - `Levels.fyi`: re-scoped after drifting into plan-only work; adapter and detail-enrichment work remains active
- Integrated the remaining two launch-build sources on the controller branch:
  - `YC Jobs`: direct HTTP adapter, parser, capture helpers, registration, schema acceptance, cache policy, and dashboard source-row contract now pass in targeted verification
  - `Levels.fyi`: direct HTTP adapter, bounded detail enrichment, registration, schema acceptance, cache policy, and dashboard source-row contract now pass in targeted verification
- Closed the final support blocker for new source types by fixing dashboard row serialization so newly added HTTP-direct sources always surface one accountability object (`criteriaAccountability` or `formatterDiagnostics`) and therefore satisfy the shared `/api/dashboard` contract.
- Added a controller oversight rule: returned lane output must be integrated, explicitly deferred, re-scoped, or used to reassign capacity in the same cycle. Passive polling is not acceptable once workers have started returning results.
- Updated the controller execution model so each MVP source lane is now required to return reusable source-addition artifacts, not just adapter code. This keeps the six-source MVP aligned with the later `add a source` goal instead of creating six bespoke integrations.

## Corrections
- Process regression: roadmap artifacts were not being updated consistently while backlog and ExecPlan work continued.
- Correction applied:
  - `docs/roadmap/decision-log.md`
  - this daily progress note
  - `docs/learnings.md` rule to prevent recurrence
- Controller-process regression: returned lane output was not being reviewed/integrated immediately as it completed.
- Correction applied:
  - dispatch board now marks returned vs integrated lanes
  - controller now reviews and lands lane output as it arrives instead of batching it too late

## Current Branch State
- Branch: `codex/controller-source-data-quality`
- Status: controller branch with all six MVP source lanes now integrated locally; remaining launch risk is live validation, not build completeness

## Next Step
- Run live validation against the real user environment for:
  - LinkedIn import quality / scores
  - Indeed degraded challenge handling
- Then move to the next approved post-source-quality items:
  - canonical title-family bucketing
  - missing full job details
