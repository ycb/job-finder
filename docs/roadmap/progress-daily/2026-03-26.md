- Completed product-integration parity for the two new MVP sources:
  - `YC Jobs` now behaves as an auth-required product source while still using a direct HTTP adapter path
  - `Levels.fyi` remains a standard no-auth direct HTTP source
- Extended shared search/source reporting logic so `YC Jobs` and `Levels.fyi` register as first-class source kinds instead of falling through as `unknown`.
- Added internal-only source novelty accounting with the approved default baseline:
  - `LinkedIn + Indeed`
- Wired novelty diagnostics into dashboard source rows without exposing any new end-user UI.
- Added reusable source-type artifacts for both sources:
  - [YC Jobs notes](/Users/admin/.codex/worktrees/51f6/job-finder/docs/roadmap/yc-jobs-http-direct-notes.md)
  - [Levels.fyi notes](/Users/admin/.codex/worktrees/51f6/job-finder/docs/roadmap/levelsfyi-http-direct-notes.md)
- Verification completed:
  - `npm run dashboard:web:build`
  - `node --test test/dashboard-api-contract.test.js test/source-access.test.js test/review-searches-react-logic.test.js test/review-react-onboarding-model.test.js test/source-novelty.test.js test/yc-jobs.test.js test/yc-capture.test.js test/yc-source-registration.test.js test/levelsfyi-jobs.test.js test/levelsfyi-source-registration.test.js`
- Fixed two QA regressions in the source-management surface:
  - auth failures during modal sign-in checks no longer need a redundant destructive toast path
  - source rows now aggregate legacy source IDs into the current MVP source slate so cumulative counts and last-run timestamps do not collapse to fresh-config-only values
- Tightened the public row contract:
  - `Found` is now additive (`filtered + dupes + imported`) instead of `imported/?`
  - source rows expose `foundCount` explicitly for both React and server-rendered tables
- Verification completed for the source-row semantics pass:
  - `node --test test/review-searches-react-logic.test.js test/review-source-data-quality.test.js test/dashboard-api-contract.test.js test/yc-source-registration.test.js test/levelsfyi-source-registration.test.js`
  - `npm run dashboard:web:build`
- Root cause follow-up: the first alias-aggregation fix still failed live because `validateSources()` stripped `legacySourceIds` during config loading. Preserved the field through schema validation so the running dashboard can aggregate historical source IDs in the same way the tested source-library helpers do.
- Started the additive source-metrics accounting fix:
  - `source_run_deltas` now persists `found_count`, `filtered_count`, and `deduped_count` alongside `imported_count`
  - dashboard rows and totals now use cumulative persisted metrics when available
  - historical rows without persisted filter/dedupe history render those metrics as unavailable instead of fake zeros
- Verification completed for the persisted source-metrics pass:
  - `node --test test/run-deltas.test.js test/review-searches-react-logic.test.js test/review-source-data-quality.test.js test/dashboard-api-contract.test.js`
  - `npm run dashboard:web:build`
