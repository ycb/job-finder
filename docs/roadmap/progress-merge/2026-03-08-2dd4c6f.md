# Merge-to-Main Update - 2026-03-08 (`2dd4c6f`)

## Summary

Completed the Phase 1 Source Contracts diagnostics slice (`W1-04`) by persisting drift artifacts with field-level mismatch context and wiring diagnostics execution into the sync ingest path.

## Shipped This Merge

- Added field-level drift mismatch context in contract drift rows (`coverageMismatches`) with threshold and rolling/latest coverage values.
- Added persisted diagnostics artifact writer and runner in source contracts module:
  - timestamped and `latest.json` outputs under `data/quality/contract-drift` by default
  - shared `runSourceContractDiagnostics(...)` entrypoint
- Wired contract diagnostics into:
  - `jf check-source-contracts` output path reporting
  - `jf sync` ingest path (contract diagnostics run after source ingest; quality summary includes diagnostics path)
- Added dedicated tests for diagnostics behavior:
  - healthy payload (no mismatches)
  - missing required field coverage (field-level mismatch context)
  - persisted diagnostics artifact generation

Verification:
- `npm test` => `167` pass, `0` fail.

## Backlog Status Changes

- `W1-04` moved to `Completed`.
- Active dispatch moved from `W1-04` to `W1-05`.
- `P0` source-shape contracts library moved to `Completed`.

## New Dependencies or Blockers

- No new blockers introduced.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction path.

## Decisions Logged

- Logged autonomous decision that `W1-04` is complete on merge `2dd4c6f` and active dispatch is now `W1-05`.

## Next 1-3 Prioritized Moves

1. Execute `W1-05`: complete dashboard surfacing for persisted formatter diagnostics.
2. Execute `W1-06`: finish multi-keyword criteria flow through URL construction.
3. Keep `W1-07` schema/metering lane warm while criteria-fidelity slice lands.
