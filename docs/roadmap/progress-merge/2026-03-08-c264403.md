# Merge-to-Main Update - 2026-03-08 (`c264403`)

## Summary

Completed Operations Metrics `W1-07` by adding a canonical analytics event schema, channel/identity tags, PostHog mapping, and durable local counters.

## Shipped This Merge

- Added canonical analytics contract:
  - schema versioning
  - channel taxonomy (`terminal`, `dashboard`, `codex`, `claude`)
  - identity mode taxonomy (`machine_hash`, `anonymous_session`)
  - canonical-to-PostHog event mapping
- Added runtime analytics client with:
  - envelope creation + validation
  - local event log persistence
  - local counter persistence by event and channel
  - PostHog `/capture` transport mapping
- Wired instrumentation for:
  - terminal CLI pipeline/events
  - dashboard API mutation/run/status actions
- Added docs for the event contract and mapping.
- Added tests for:
  - envelope serialization and event registry
  - counter updates and persisted metrics
  - PostHog payload mapping behavior

Verification:
- `npm test` => `175` pass, `0` fail.

## Backlog Status Changes

- `W1-07` moved to `Completed`.
- Active dispatch moved from `W1-07` to `W1-08`.
- `P1` tooling ownership/metering baseline item moved to `Completed`.

## New Dependencies or Blockers

- No new blockers introduced.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction path.

## Decisions Logged

- Logged autonomous decision to treat Codex/Claude as first-class channels in schema now, with runtime producers to be wired as those surfaces ship.

## Next 1-3 Prioritized Moves

1. Execute `W1-08` status-aware retention policy defaults and wiring.
2. Start value metrics + caps + donation verification on top of the analytics baseline.
3. Continue P0 unblocker path for Built In salary extraction.
