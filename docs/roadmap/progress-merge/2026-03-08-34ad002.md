# Merge-to-Main Update - 2026-03-08 (`34ad002`)

## Summary

Completed Operations Metrics `W1-08` by implementing status-aware retention defaults, policy loading/inspection, and cleanup-audit wiring in sync flows.

## Shipped This Merge

- Added retention policy config module:
  - default policy (`auto-delete ON`)
  - status TTL defaults:
    - `new=30`
    - `viewed=45`
    - `skip_for_now=21`
    - `rejected=14`
    - `applied=never`
  - policy path resolution and loading/saving behavior
- Added retention cleanup module:
  - status-aware deletion by TTL
  - explicit `applied` protection behavior
  - cleanup audit writer with deleted-by-status and protected counts
- Wired cleanup into:
  - CLI `sync` flow
  - dashboard `runSyncAndScore` flow
- Added retention policy inspect command:
  - `jf retention-policy`
- Added tests for:
  - policy defaults + override persistence
  - cleanup behavior and applied preservation
  - cleanup audit persistence

Verification:
- `npm test` => `179` pass, `0` fail.

## Backlog Status Changes

- `W1-08` moved to `Completed`.
- Phase 1 Wave 1 queue now complete.
- `P1` local storage retention controls moved to `Completed`.

## New Dependencies or Blockers

- No new blockers introduced by this merge.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction.

## Decisions Logged

- Logged autonomous decision to treat retention cleanup as part of both terminal and dashboard sync paths, not as a separate scheduled job in MVP.

## Next 1-3 Prioritized Moves

1. Start value metrics + usage caps + donation verification sequencing.
2. Unblock Built In salary extraction dependency for full-JD page-level pass.
3. Continue remaining in-progress core items: search controls and net-new refresh behavior.
