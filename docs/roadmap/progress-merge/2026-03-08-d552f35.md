# Merge-to-Main Update - 2026-03-08 (`d552f35`)

## Summary

Completed the Criteria Fidelity formatter diagnostics slice (`W1-05`) by persisting formatter diagnostics in source metadata and surfacing them in dashboard search status details.

## Shipped This Merge

- Added durable `formatterDiagnostics` source metadata (`unsupported`, `notes`) in source derivation flow.
- Extended source schema validation to support and validate `formatterDiagnostics`.
- Surfaced formatter diagnostics in dashboard source rows (`formatter:` status detail in Searches table).
- Added tests for:
  - source metadata persistence and stale-reset behavior
  - schema validation (valid + invalid diagnostics shapes)
  - dashboard render path usage of formatter diagnostics

Verification:
- `npm test` => `169` pass, `0` fail.

## Backlog Status Changes

- `W1-05` moved to `Completed`.
- Active dispatch moved from `W1-05` to `W1-06`.
- `P1` persist formatter diagnostics item moved to `Completed`.

## New Dependencies or Blockers

- No new blockers introduced.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction path.

## Decisions Logged

- Logged autonomous decision that `W1-05` is complete on merge `d552f35` and active dispatch is now `W1-06`.

## Next 1-3 Prioritized Moves

1. Execute `W1-06`: complete multi-keyword criteria flow through URL/search handling.
2. Continue `W1-07` event-schema/metering contract hardening in operations lane.
3. Re-check P0 full-JD dependency path once W1-06 stabilizes.
