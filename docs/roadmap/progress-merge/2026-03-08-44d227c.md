# Merge-to-Main Update - 2026-03-08 (`44d227c`)

## Summary

Completed Criteria Fidelity `W1-06` by normalizing comma-delimited keywords end-to-end through criteria persistence and source URL query construction.

## Shipped This Merge

- Added shared keyword normalization helper:
  - split on commas
  - trim whitespace
  - dedupe repeated keywords
- Wired normalization into `validateSearchCriteria` so persisted criteria are canonicalized.
- Updated source URL builders to consume normalized keyword sets for query construction.
- Documented current behavior:
  - comma keywords map to `AND`-style intent in query construction
  - explicit `OR` behavior remains in the follow-up search-controls item
- Added/updated tests for:
  - criteria persistence normalization
  - Indeed query formatting with comma keywords
  - Google query formatting with comma keywords

Verification:
- Lane full suite (`.codex/worktrees/phase1-criteria-fidelity`):
  - `npm test` => `170` pass, `0` fail.
- Main targeted regression set:
  - `npm test -- test/search-url-builder.test.js test/search-criteria-config.test.js test/source-search-criteria-bootstrap.test.js test/source-criteria-accountability.test.js test/sources-schema.test.js` => pass.

## Backlog Status Changes

- `W1-06` moved to `Completed`.
- Active dispatch moved from `W1-06` to `W1-07`.
- `P1` comma-separated multi-keyword item moved to `Completed`.

## New Dependencies or Blockers

- No new blockers introduced by this merge.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction.

## Decisions Logged

- Logged autonomous decision to treat comma keyword normalization as `AND`-style by default and defer explicit `OR` controls to the search-controls expansion item.

## Next 1-3 Prioritized Moves

1. Execute `W1-07` canonical event schema + PostHog channel mapping.
2. Execute `W1-08` status-aware retention policy wiring.
3. Continue P0 unblocker work on Built In salary extraction path.
