# Merge-to-Main Update - 2026-03-11 (`F1-A Infra`)

## Summary

Implemented the infra scaffold for a React dashboard frontend (Vite + React), added build output under `src/review/web/dist`, and integrated review-server static hosting behind `JOB_FINDER_DASHBOARD_UI=react` with legacy fallback preserved.

## Verification Commands

- `node --test test/review-narrata-flag.test.js` -> pass (`38` tests, `0` fail)
- `node --test test/review-refresh-ui-copy.test.js` -> pass (`3` tests, `0` fail)
- `npm test` -> pass (`241` tests, `0` fail)

## React Shell Serve Evidence

- Env flag used: `JOB_FINDER_DASHBOARD_UI=react`
- Validation URL: `http://127.0.0.1:4411`
- `curl http://127.0.0.1:4411` returned React-shell index markers:
  - `<script type="module" crossorigin src="/assets/index-B4S-J6mv.js"></script>`
  - `<link rel="stylesheet" crossorigin href="/assets/index-CEp2Hh5v.css">`
  - `<div id="root"></div>`
- `curl http://127.0.0.1:4411/api/dashboard` returned JSON payload (API route still active in React mode).

## Legacy Fallback Evidence

- Validation URL: `http://127.0.0.1:4412` (no `JOB_FINDER_DASHBOARD_UI` flag)
- `curl` response includes legacy inline renderer marker:
  - `const narrataConnectEnabled = false;`

## Notes

- Port `4311` was already occupied in this environment during verification, so proof URLs used ports `4411` (React mode) and `4412` (legacy fallback).
