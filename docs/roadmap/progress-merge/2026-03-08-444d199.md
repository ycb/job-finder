# Merge-to-Main Update - 2026-03-08 (`444d199`)

## Summary

Completed the Phase 1 Source Contracts schema slice (`W1-03`) by formalizing contract search/extraction shape semantics and strengthening drift gate threshold handling.

## Shipped This Merge

- Expanded source contract normalization and validation schema:
  - explicit `searchParameterShape` buckets (`required`, `optional`, `uiDrivenOnly`, `supported`, `unsupported`)
  - explicit extraction metadata buckets (`requiredMetadata`, `optionalMetadata`)
  - quality threshold contract support (`rollingMinCoverage`, `minCoverageByField`, `minSampleSize`)
- Added source-specific contract override resolution (`sourceType + sourceId` preference before type default).
- Updated drift evaluation to honor configured threshold semantics safely and preserve fallback gate behavior when thresholds are unset.
- Added/extended tests for:
  - overlap rejection in search parameter buckets
  - sourceId-specific contract resolution
  - drift gate threshold behavior regression

Verification:
- `npm test` => `164` pass, `0` fail.

## Backlog Status Changes

- `W1-03` moved to `Completed`.
- Active dispatch moved from `W1-03` to `W1-04`.
- `P0` source-shape contracts backlog item moved from `Planned` to `In progress`.

## New Dependencies or Blockers

- No new blockers introduced.
- Existing blocker remains: page-level full-JD pass depends on Built In salary extraction path.

## Decisions Logged

- Logged autonomous decision that `W1-03` is complete on merge `444d199` and active dispatch is now `W1-04`.

## Next 1-3 Prioritized Moves

1. Execute `W1-04`: ingest-path contract loader + persisted drift diagnostics hardening.
2. Keep `W1-07` telemetry schema task in parallel readiness while `W1-04` lands.
3. Re-check `P0` full-JD dependency after contract diagnostics persistence is in place.
